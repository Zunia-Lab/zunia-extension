# Zunia extension — local secrets (do not commit)

# WalletConnect Cloud project (same ID as mobile + dashboard)
WXT_WALLETCONNECT_PROJECT_ID=

# Optional: override chain registry CDN
# WXT_CHAIN_REGISTRY_URL=https://raw.githubusercontent.com/Zunia-Lab/zunia-chain-registry/main
